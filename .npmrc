# Playwright browsers are baked into the Railway image and the CI runner.
# Set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 in those environments instead of here,
# so a fresh local clone still gets a browser.
link-workspace-packages=true
