{
  "name": "@botready/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts, the check catalog, the pure scoring function and the fix-file generators. Zero runtime dependencies on purpose: the web app, the worker and the tests all import this.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./checks.json": "./checks.json"
  },
  "files": ["src", "checks.json"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {}
}
